# === crear ambiente virtual ===
python3 -m venv .venv

# === activar ambiente virtual ===
source .venv/bin/activate

# === instalar librerias ===
pip3 install -r requirements.txt

# === COMANDOS EN GITHUB ===
git status

# Guardar cambios
git add

# Comentar cambios
git commit -m "comentario que deseamos escribir"

# Actualizar branch
git push origin main
